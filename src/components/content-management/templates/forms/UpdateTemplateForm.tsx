import { cn } from '@/lib/utils';

interface UpdateTemplateFormProps {
  className?: string;
}

export const UpdateTemplateForm = ({ className }: UpdateTemplateFormProps) => {
  return (
    <div className={cn('flex flex-col flex-1 overflow-hidden', className)}>
      {/* TODO: Implement update template form */}
    </div>
  );
};
